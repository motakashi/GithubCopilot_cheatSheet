# カスタムエージェント

このページは、GitHub Copilot のカスタムエージェントについて、公式ドキュメントの内容に合わせて整理した入門メモです。

- カスタムエージェントとは何か
- どこに置くか
- ファイルの書き方
- 作成方法と使い方
- 注意点

## 目次

- [1. カスタムエージェントとは](#1-カスタムエージェントとは)
- [2. どこに置くか](#2-どこに置くか)
- [3. ファイルの書き方](#3-ファイルの書き方)
- [4. 作成方法](#4-作成方法)
- [5. 使い方](#5-使い方)
- [6. プロンプトファイルとの違い](#6-プロンプトファイルとの違い)
- [7. 注意点](#7-注意点)
- [8. 最小テンプレート](#8-最小テンプレート)
- [9. Tree 構造の例](#9-tree-構造の例)

---

## 1. カスタムエージェントとは

カスタムエージェントは、Copilot エージェントの特殊なバージョンです。
独自のワークフロー、コーディング規則、ユースケースに合わせて調整でき、同じ指示を毎回繰り返さずに使えます。

公式ドキュメントでは、カスタムエージェントは次のようなものとして説明されています。

- 標準やチーム固有のプラクティスに従う調整済みのチームメイトのように動作する
- プロンプト、ツール、MCP サーバーを含む Markdown ファイルで定義する
- タスクや問題に割り当てると、その定義に従って振る舞う

イメージとしては、"特定の役割に最適化された Copilot の専用設定" です。

簡単な例:

```text
README を改善するエージェント
```

```text
実装計画を作るエージェント
```

```text
テスト観点に特化したエージェント
```

---

## 2. どこに置くか

リポジトリレベルのカスタムエージェントは、`.github/agents/CUSTOM-AGENT-NAME.agent.md` に置きます。

基本の配置例:

```text
project/
├─ .github/
│  └─ agents/
│     ├─ readme-creator.agent.md
│     ├─ implementation-planner.agent.md
│     └─ test-specialist.agent.md
└─ README.md
```

公式ドキュメントでは、組織レベルや企業レベルでも使える仕組みが案内されています。
ただし、このページではまずリポジトリレベルの使い方を押さえるのが分かりやすいです。

配置の考え方は次のとおりです。

- リポジトリ固有の役割なら `.github/agents`
- 組織全体で共有するなら上位レベルの管理機能

---

## 3. ファイルの書き方

カスタムエージェントは、YAML frontmatter を含む Markdown ファイルです。

最小構成は次の 3 要素です。

- `name` は省略可能。省略時はファイル名が表示名になる
- `description` は必須
- 本文にはエージェントの動作や専門知識を書く

必要に応じて、次も追加できます。

- `tools`
- `mcp-servers`
- `model`
- `target`
- `argument-hint`
- `handoffs`

最小例:

```md
---
name: readme-creator
description: README の作成と改善を担当するエージェント
tools: ["read", "search", "edit"]
target: github-copilot
---

You are a documentation specialist focused on README files.
Your scope is limited to README files and related documentation only.

Follow these rules:
- Create and improve README files with clear project descriptions
- Structure sections logically: overview, installation, usage, contributing
- Write scannable content with proper headings and formatting
- Use relative links for repository files
```

ポイントは次のとおりです。

- `name` を省略すると、ファイル名が識別子と表示名になる
- `description` はエージェントの目的を短く書く
- `tools` は使えるツールを絞るために使う
- `mcp-servers` でこのエージェント専用の MCP サーバーを追加できる
- `target` で VS Code 用か GitHub Copilot 用かを切り替えられる

---

## 4. 作成方法

公式ドキュメントでは、次の流れで作成します。

1. GitHub の Agents タブまたは VS Code のカスタムエージェント作成画面を開く
2. リポジトリを選ぶ
3. 必要ならブランチを選ぶ
4. `.github/agents` に `.agent.md` ファイルを作る
5. 名前、説明、ツール、MCP、本文を設定する
6. 既定ブランチにマージする

VS Code では、Agent Customizations エディターや `Chat: New Custom Agent` コマンドから作れます。

生成補助も使えます。

- `/create-agent` と入力して役割を説明すると、AI が下書きを作れる
- 既存の会話からエージェント化することもできる

---

## 5. 使い方

カスタムエージェントを作成すると、利用できる場所で選択して使えます。

公式ドキュメントでは、次の場所で使えると案内されています。

- GitHub の Agents タブやパネル
- Issue の割り当て
- Pull Request
- VS Code などの IDE
- GitHub Copilot CLI

使い方の基本は次のとおりです。

- 目的に合ったエージェントを選ぶ
- その役割に沿った依頼をする
- 必要なら別のエージェントに切り替える

依頼例:

```text
readme-creator を使って README の構成を改善して
```

```text
implementation-planner を使って、実装手順を Markdown でまとめて
```

```text
test-specialist を使って、足りないテスト観点を整理して
```

ハンドオフを使うと、1 つのエージェントから別のエージェントに段階的に移れます。

---

## 6. プロンプトファイルとの違い

カスタムエージェントは、プロンプトファイルよりも固定された persona を持つのが特徴です。

ざっくり分けると、次のようになります。

- カスタムエージェント: 役割、ツール、モデル、ハンドオフまで含めた再利用可能な persona
- プロンプトファイル: 1 回の作業向けの再利用可能な指示
- 旧チャットモード: 以前の呼び方で、現在は `.agent.md` が主流

使い分けの目安は次のとおりです。

- 継続的に同じ役割を使うならカスタムエージェント
- 1 回だけの作業ならプロンプトファイル
- 旧 `.chatmode.md` を使っているなら `.agent.md` に移行する

---

## 7. 注意点

カスタムエージェントは便利ですが、作り方を間違えると逆に使いづらくなります。

注意したい点は次のとおりです。

- 役割を欲張りすぎない
- `tools` を広げすぎない
- `description` を曖昧にしない
- 本文に矛盾する指示を書かない
- 1 つのエージェントに全部詰め込まない

悪い例:

```md
You are a helpful agent that can do everything.
```

良い例:

```md
You are a documentation specialist focused on README files.
Your scope is limited to documentation files only.
```

セキュリティが気になる作業では、読み取り専用のツールだけに絞ると安全です。
公式ドキュメントでも、最小権限の考え方が推奨されています。

---

## 8. 最小テンプレート

最初は、次のような最小テンプレートから始めると運用しやすいです。

```md
---
name: readme-creator
description: README を改善するエージェント
tools: ["read", "search", "edit"]
---

You are a documentation specialist focused on README files.
Your scope is limited to README files and related documentation only.

Follow these rules:
- Write clear and scannable text
- Keep sections well structured
- Prefer relative links for repository files
```

これだけでも、用途を分けたエージェントとして十分に使えます。

---

## 9. Tree 構造の例

リポジトリで複数のカスタムエージェントを管理するなら、次のような Tree が分かりやすいです。

```text
project/
├─ .github/
│  └─ agents/
│     ├─ readme-creator.agent.md
│     ├─ implementation-planner.agent.md
│     └─ test-specialist.agent.md
├─ README.md
├─ docs/
└─ tests/
```

分ける基準はシンプルです。

1. 役割が違うか
2. 使うツールが違うか
3. 出力形式が違うか

この 3 つのどれかが違うなら、別エージェントに分けるのが自然です。

---

## 参考リンク

- [カスタム エージェントについて（GitHub Docs）](https://docs.github.com/ja/copilot/concepts/agents/cloud-agent/about-custom-agents)
