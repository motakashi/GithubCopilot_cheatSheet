# GitHub Copilot カスタム命令マニュアル

このページは、VS Code で使う GitHub Copilot のカスタム命令について、次の 2 点に絞ってまとめた実践ガイドです。

- 使い方
- 新規作成方法（書式）

## 目次

- [1. カスタム命令とは](#1-カスタム命令とは)
- [2. 使い方（基本）](#2-使い方基本)
- [3. 新規作成方法（書式）](#3-新規作成方法書式)
- [4. 書式のポイント](#4-書式のポイント)
- [5. まずこれだけで始める最小構成](#5-まずこれだけで始める最小構成)
- [6. 動作確認テンプレート](#6-動作確認テンプレート)
- [7. うまくいかない時のチェック](#7-うまくいかない時のチェック)
- [8. VS Code での具体的な使い方（実操作）](#8-vs-code-での具体的な使い方実操作)
- [9. 新規作成用の書式テンプレート集](#9-新規作成用の書式テンプレート集)
- [10. 公式ドキュメント補足（保存場所と参考リンク）](#10-公式ドキュメント補足保存場所と参考リンク)

---

## 1. カスタム命令とは

カスタム命令は、Copilot に対して「このプロジェクトではこう振る舞ってほしい」を事前に伝える仕組みです。

例えば次の内容を固定ルール化できます。

- コメントは日本語で書く
- テストを必ず提案する
- 既存コーディング規約に合わせる
- 変更理由を先に説明する

毎回チャットで同じ指示を書かなくてよくなるため、回答のブレを減らせます。

---

## 2. 使い方（基本）

### 2-1. どこで使うか

主に次の 3 パターンで運用します。

1. 個人向け（自分だけ）
2. リポジトリ共通（チーム共有）
3. ファイル種別ごと（例: Markdown だけ別ルール）

### 2-2. 実務での使い分け

- 個人向け: 口調、回答形式、説明の粒度など
- リポジトリ共通: 命名規則、レビュー観点、禁止事項
- ファイル種別ごと: ドキュメント用、テスト用、設定ファイル用ルール

### 2-3. チャットでの呼び出し方

カスタム命令は自動で参照される前提ですが、次のように対象を明示すると精度が上がります。

```text
#file:README.md このファイル向けのカスタム命令に従って改善して
```

```text
@workspace このリポジトリの命名規約に従ってリファクタして
```

命令ファイルそのものを「この1ファイルだけ使う」と厳密指定するのは難しいですが、次の定型文で意図的に寄せることはできます。

定型文（リポジトリ全体ルールを優先させたいとき）:

```text
@workspace このリポジトリのカスタム命令に従って回答してください。特に .github/copilot-instructions.md の方針を優先し、変更理由を先に3行以内で説明してください。
```

定型文（特定ファイルのパス別命令を使いたいとき）:

```text
#file:README.md このファイルに適用される instructions に従って編集してください。見出し構造は維持し、手順は番号付きで出してください。
```

定型文（確実に反映させたいとき）:

```text
このリポジトリのカスタム命令に従ってください。念のため次の制約も必ず守ってください: 変更理由を先に説明 / 公開API変更は理由を明記 / 最後に確認項目を3つ列挙。
```

使い分けの目安:

1. まず `@workspace` か `#file:` で対象を明示する
2. 反映が弱いと感じたら、重要制約だけ依頼文に再掲する
3. 長い再掲は避け、3項目程度に絞る

---

## 3. 新規作成方法（書式）

## 3-1. リポジトリ共通ルールを作る

最も使いやすいのは、リポジトリに次のファイルを置く方法です。

- `.github/copilot-instructions.md`

最小テンプレート:

```md
# Copilot Instructions

## このリポジトリで必ず守ること
- 既存の命名規則に合わせる
- 変更の影響範囲を先に説明する
- 変更後に必要なテスト観点を必ず列挙する

## コード編集ポリシー
- 不要な大規模リフォーマットはしない
- 既存 API をむやみに変更しない
- コメントは必要最小限にする

## 回答フォーマット
- 先に結論
- 次に変更点
- 最後に確認項目
```

## 3-2. ファイル種別ごとのルールを作る

より細かく制御したい場合は、`.instructions.md` ファイルを作成し、先頭に Front Matter を付けます。

サンプル: Markdown 用ルール

ファイル名例:
- `docs.instructions.md`

内容例:

```md
---
applyTo: "**/*.md"
---

見出し構造を崩さずに編集する。
手順は箇条書きで書く。
設定例は必ずコードブロックで併記する。
```

サンプル: テストコード用ルール

ファイル名例:
- `tests.instructions.md`

内容例:

```md
---
applyTo: "**/*.{test,spec}.{ts,tsx,js,jsx}"
---

Arrange-Act-Assert の順で記述する。
正常系と異常系を最低 1 ケースずつ提案する。
テスト名は日本語で意図が分かるように書く。
```

---

## 4. 書式のポイント

`.instructions.md` を使う場合の注意点:

- 先頭の `---` と末尾の `---` を忘れない
- `applyTo` は glob 形式で書く
- 曖昧な指示より、行動可能な文にする

良い例:

- 「変更理由を 3 行以内で先に説明する」
- 「公開 API のシグネチャを変更する場合は理由を書く」

避けたい例:

- 「いい感じに直す」
- 「必要なら対応する」

---

## 5. まずこれだけで始める最小構成

1. `.github/copilot-instructions.md` を作る
2. 共通ルールを 5 個以内に絞る
3. 必要なら `docs.instructions.md` などを追加する

最初からルールを増やしすぎると、指示同士が競合しやすくなります。
まずは小さく始めて、実運用で不足分を追加するのがおすすめです。

---

## 6. 動作確認テンプレート

作成後は次の依頼で確認できます。

```text
#file:README.md このリポジトリのカスタム命令に従って、章立てと手順を改善して
```

```text
@workspace 既存規約に従って命名だけを修正し、理由を先に説明して
```

---

## 7. うまくいかない時のチェック

- ファイル名が正しいか
- `.instructions.md` の Front Matter が壊れていないか
- `applyTo` の対象パターンが狭すぎないか
- ルール同士が矛盾していないか

以上を直すだけで、反映精度が上がることが多いです。

---

## 8. VS Code での具体的な使い方（実操作）

### 8-1. リポジトリ共通ルールを効かせる手順

1. リポジトリ直下に `.github/copilot-instructions.md` を作成する
2. プロジェクト全体に適用したいルールを書く
3. Copilot Chat で `@workspace` を付けて依頼する
4. 期待通りの出力か確認し、必要なら命令文を短く具体化する

依頼例:

```text
@workspace このリポジトリのカスタム命令に従って、変更理由を先に説明してから実装案を出して
```

### 8-2. ファイル種別ルールを効かせる手順

1. `*.instructions.md` を作成する
2. 先頭に Front Matter で `applyTo` を定義する
3. 対象ファイルを開いて `#file:` 指定で依頼する

依頼例:

```text
#file:README.md このファイルの命令に従って、手順を番号付きで整理して
```

### 8-3. 運用のコツ

- 1 命令 1 意図で書く（長文に詰め込みすぎない）
- 「何を」「どの順で」「どこまで」を明示する
- 反映が弱い場合は、依頼文でも同じ制約を短く再指定する

### 8-4. 命令が長くなるときの注意点

1. 最上位ルールは 5 個以内に固定する
2. 1 行 1 意図で書く（1 行に複数条件を混ぜない）
3. 抽象語を避ける（「適切に」より「3行以内で説明」）
4. 競合する命令を置かない（例: 「詳しく説明」と「常に簡潔に」）
5. 変更理由・制約・出力形式の 3 要素を明示する

悪い例:

```text
必要に応じていい感じに実装し、説明は適宜調整する。
```

良い例:

```text
変更前に影響範囲を3行以内で説明し、最後に確認項目を3つ列挙する。
```

### 8-5. 別レイヤー化の考え方と具体手順

命令が増える場合は、追加ではなく次の 3 レイヤーに分けると安定します。

1. Personal: 口調、言語、回答スタイル
2. Repository-wide: 全体共通の規約、禁止事項、必須チェック
3. Path-specific: 特定ディレクトリや拡張子だけの詳細ルール

分け方の基準:

1. どの作業でも必須なら Repository-wide
2. 特定ファイルだけなら Path-specific
3. 個人の好みなら Personal

具体手順:

1. 既存命令を一覧化する
2. 「全体共通」と「対象限定」に仕分ける
3. 全体共通だけを `.github/copilot-instructions.md` に残す
4. 対象限定を `.github/instructions/NAME.instructions.md` に移す
5. 1 週間運用し、重複・競合を削る

設定イメージ:

```text
Personal
	- 日本語で回答
	- 結論を先に書く

Repository-wide (.github/copilot-instructions.md)
	- 変更前に影響範囲を説明
	- 公開 API 変更時は理由を明記
	- 変更後にテスト観点を列挙

Path-specific (.github/instructions/frontend.instructions.md)
	- applyTo: "frontend/**/*.{ts,tsx},**/*.css"
	- UI 変更時はアクセシビリティ影響を説明
```

### 8-6. 5個を超えそうなときの判断ルール

1. 新規追加前に、既存の重複行を 1 つ削除する
2. 5 個を超える内容は Path-specific に逃がす
3. それでも増える場合は「チェックリスト化」して別ファイル化する

---

## 9. 新規作成用の書式テンプレート集

### 9-1. `.github/copilot-instructions.md` テンプレート

```md
# Copilot Instructions

## Must
- 既存の設計方針と命名規則に合わせる
- 変更前に影響範囲を短く説明する
- 変更後に確認手順またはテスト観点を示す

## Must Not
- 無関係なファイルをまとめて整形しない
- 依頼範囲外のリネームや API 変更をしない

## Response Style
- 先に結論、次に変更内容、最後に確認事項
```

### 9-2. `*.instructions.md` テンプレート

```md
---
applyTo: "**/*.md"
---

見出し階層を維持する。
手順は番号付きリストで書く。
設定例は必ずコードブロックで併記する。
```

### 9-3. 複数拡張子への適用テンプレート

```md
---
applyTo: "**/*.{ts,tsx,js,jsx}"
---

既存の ESLint ルールに従う。
関数には副作用の有無を明確にする。
公開関数を変更する場合は理由を先に説明する。
```

### 9-4. 命令文の書き方（推奨フォーマット）

次の形にすると、曖昧さを減らせます。

```text
[対象] に対して、[制約] を守り、[出力形式] で回答する。
```

例:

```text
README の手順説明に対して、既存見出しを維持し、設定例をコードブロック付きで回答する。
```

---

## 10. 公式ドキュメント補足（保存場所と参考リンク）

### 10-1. 個人・リポジトリ・組織の保存場所

- Personal instructions: GitHub.com の Copilot Chat 内の個人設定
- Repository instructions（リポジトリ全体）: `.github/copilot-instructions.md`
- Repository instructions（パス別）: `.github/instructions/NAME.instructions.md`
- Organization instructions: GitHub.com の Organization Settings > Copilot > Custom instructions

### 10-2. 参考リンク

- https://docs.github.com/ja/copilot/how-tos/copilot-on-github/customize-copilot/add-custom-instructions/add-personal-instructions
- https://docs.github.com/ja/copilot/how-tos/copilot-on-github/customize-copilot/add-custom-instructions/add-repository-instructions
- https://docs.github.com/ja/copilot/how-tos/copilot-on-github/customize-copilot/add-custom-instructions/add-organization-instructions
